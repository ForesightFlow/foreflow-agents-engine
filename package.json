{
  "name": "foreflow-agents-engine",
  "version": "0.4.1",
  "description": "Operational infrastructure for ForeFlow agents — registration, scheduling, healthcheck, and deployment",
  "type": "module",
  "bin": {
    "foreflow-engine": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test 'dist/tests/*.test.js'",
    "build:test": "tsc && node --test 'dist/tests/*.test.js'",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "foresight-arena": "^0.1.6",
    "twitter-api-v2": "^1.29.0",
    "viem": "^2.27.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "author": "Maksym Nechepurenko, Pavel Shuvalov",
  "license": "MIT"
}
